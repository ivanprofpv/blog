<br>
<% if user_signed_in? %>
  <%= link_to "My post", my_path, class: 'btn btn-info btn-sm' %>
<% end %>
<br>
<% @posts.each do |post| %>
  <%= link_to post_path(post) do %>
    <li><%= post.title %></li>
  <% end %>
<% end %>
<div class="pagination"><%= paginate @posts %></div>
